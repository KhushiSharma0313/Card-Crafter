import React from 'react'

const preview = ({Values}) => {
    
    return( 
    <section className = "Preview">
        <h2>Preview Pane</h2>
      
    </section>
    );
    
    
  
};

export default preview
